{
  "name": "@geolab/tool-core",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Engine-agnostic GeoLab abstractions: Tool / Layer / Pipeline / Project + registry + runner + provenance.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
