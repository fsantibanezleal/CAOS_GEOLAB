{
  "name": "@geolab/adapter-geolibre",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Adapter mapping geolibre-wasm (WhiteboxTools + GeoLibre, MIT) tools into the GeoLab Tool registry.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@geolab/tool-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "comments": "geolibre-wasm is loaded at RUNTIME and injected (dependency injection), so this package does not statically depend on it — the engine is swappable and the build stays light."
}
