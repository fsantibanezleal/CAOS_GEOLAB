{
  "name": "caos-geolab",
  "private": true,
  "version": "0.16.0",
  "description": "GeoLab — browser-native, no-install, multi-engine geospatial tool platform (ADR-0059).",
  "license": "MIT",
  "author": "Felipe Santibañez-Leal",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.7.1",
  "scripts": {
    "dev": "pnpm -C apps/web dev",
    "build": "pnpm -r build",
    "build:web": "pnpm -C apps/web build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
