# deps / build
node_modules/
dist/
build/
*.tsbuildinfo
.vite/
coverage/

# env / secrets (secrets live ONLY in _CAOS_MANAGE — commit .env.example only)
.env
.env.*
!.env.example

# offline pipeline (heavy data + venv kept OUT of git — ADR-0050/0055)
.venv/
.venv-pipeline/
data/raw/
data/scratch/
*.onnx.bak

# OS / editor
.DS_Store
Thumbs.db
*.log
